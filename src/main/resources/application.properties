
server.port=${PORT:8080}
server.address=0.0.0.0
spring.datasource.url=jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/test?useSSL=true&requireSSL=true
spring.datasource.username=37rZwv8GW6fYwky.root
spring.datasource.password=rV55HR4MqRksUTIO
spring.jpa.show.sql=${SHOW_SQL:true}

#spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.javax.persistence.validation.mode=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#Why do we need above rule ? See PersonRepository file for its theory

spring.data.rest.basePath=/data-api

# We can only print the log statements of type severity, Debug & above severity within the code
# that is present inside my spring framework, but not the code that i have written inside my
# DashBoardController

#debug=true
#trace=true

logging.level.root=INFO

# explicitly allow INFO logging in your controllers
logging.level.com.whatsapp.controller=INFO

spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:20MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:20MB}


#logging.level.com.whatsapp.aspects = ERROR
#logging.level.com.whatsapp.controller = TRACE

# Initialize log group whatsapp_error
# logging.group.whatsapp_error=com.whatsapp.aspects, com.whatsapp.controller
# Set log level to log group whatsapp_error
# logging.level.whatsapp_error=ERROR
